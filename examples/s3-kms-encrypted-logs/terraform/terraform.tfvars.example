aws_region = "eu-north-1"
name_prefix = "coralogix-shipper-ex"

# Leave empty for first apply; after the shipper stack exists, set this and apply again if you want an explicit key-policy statement (optional).
shipper_lambda_execution_role_arn = ""

common_tags = {
  Purpose = "coralogix-aws-shipper-example-kms"
}
